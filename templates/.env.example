# .env.example - Static environment variables template
# 
# Copy this file to .env to set up your local environment variables.
# This file is used for static values that don't require command expansion.
#
# IMPORTANT: devenv's dotenv implementation limitations:
# - Only supports simple key=value pairs
# - No variable expansion (${VAR}) or command expansion ($(cmd))
# - For dynamic values, use .envrc.local instead
# 
# For sensitive values that require command expansion (like 1Password lookups),
# use .envrc.local instead (see .envrc.local.example)

# Example static configuration
# NODE_ENV=development
# API_BASE_URL=https://api.example.com
# DATABASE_URL=postgresql://username:password@localhost:5432/dbname
# DEBUG=true